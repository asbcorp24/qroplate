# Device protocol

The QR code is used only to identify and open a local BLE connection to the device. Starting the relay requires a separate server-approved session command. The application forwards that command to the ESP32 after payment.
